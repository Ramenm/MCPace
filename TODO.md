# TODO

## Backlog assumptions

- Estimates are **coarse point ranges**, not historical throughput.
- A point is only a relative sizing unit. In this repo it roughly means **one focused half-day to two days** depending on host/tooling availability.
- ETA ranges assume **one focused maintainer**, a working Rust host, and **no major scope growth**.
- Anything that needs real client traces, additional OS-lane runs, or published-package proof stays **blocked** until those environments exist.

## Status summary

- done baseline: planner/read-path surface, hub lifecycle shell, runtime lab, archive builder, and Node/source proof loop
- in progress: moving from read-path honesty to live runtime correctness
- blocked: live runtime proof, remaining OS-lane proof, real-client compatibility traces, published release proof
- honest completion view: roughly **50%–60%** of the currently known roadmap; the repo is further along on **control plane + source proof** than on **durable live runtime + release proof**

## Recently done baseline

| ID | Status | Work | Evidence |
|---|---|---|---|
| D1 | done | Rust-native grouped read surface for `client`, `hub`, `lab`, `server`, `verify`, plus `init`, `doctor`, `version`, `candidates` | `reports/rust-command-coverage.md`, `src/` |
| D2 | done | Local file-backed hub lifecycle shell: `hub up/down/status/logs/repair` | `src/hub/`, `tests/hub_runtime.rs` |
| D3 | done | Surface-aware client catalog and routing planner | `src/client.rs`, `src/client_catalog.rs`, `docs/client-surface-matrix.md` |
| D4 | done | Runtime lab with production-like fixtures and capability-gap reporting | `src/lab.rs`, `eval/runtime-capabilities.json`, `eval/fixtures/runtime/` |
| D5 | done | Clean source archive builder and repo/source contract tests | `scripts/archive-release.mjs`, `tests/node/*.test.js` |
| D6 | done | Project-control docs and eval-governance contracts added in this pass | `TODO.md`, `STATE.md`, `DECISIONS.md`, `eval/*.json` |
| D7 | done | Fixed source-level planner/readiness regressions: restored missing `client/context` helpers, restored `lab` gap import, and tied readiness to real runtime prerequisites instead of config presence alone | `src/client/context.rs`, `src/lab/render.rs`, `src/doctor.rs` |
| D8 | done | Synced manifests/reports/archive metadata to `0.3.6` and rebuilt the clean release ZIP contract | `Cargo.toml`, `package.json`, `packages/npm/cli/package.json`, `reports/verification-latest.json`, `dist/` |
| D9 | done | Automated the verification snapshot so `reports/verification-latest.json` is generated from executed source/release checks instead of hand-edited status | `scripts/proof-report.mjs`, `tests/node/proof-report.test.js`, `reports/verification-latest.json` |
| D10 | done | Added grouped top-level `repair` as a native shorthand over `hub repair` so recovery stays on the public Rust CLI surface | `src/repair.rs`, `src/app.rs`, `tests/help_and_root.rs` |
| D11 | done | Fixed four-depth client metadata `_meta` fallback and added exhaustive permutation/combinatorial source tests for context + metadata resolution | `src/client/metadata.rs`, `src/client/context.rs` |
| D12 | done | Added a vendored binary staging lane so built host binaries can be packaged into `packages/npm/cli/vendor/<target>/`, resolved by the npm launcher, and included in release archives when present | `scripts/stage-vendored-binary.mjs`, `packages/npm/cli/lib/resolve-binary.js`, `release-manifest.json`, `scripts/archive-release.mjs` |
| D13 | done | Added vendored binary smoke verification plus current-target packaging mode to the machine-generated proof report so self-contained release claims require more than file presence | `scripts/verify-vendored-binary.mjs`, `scripts/proof-report.mjs`, `tests/node/verify-vendored-binary.test.js`, `tests/node/proof-report.test.js` |
| D14 | done | Synced command-coverage and project-control artifacts with the real Rust surface (`dashboard`, `serve`, `mcp-server`, `client install`) and added drift-catching Node contracts | `reports/rust-command-coverage.*`, `STATE.md`, `TODO.md`, `reports/tombstones.md`, `tests/node/command-coverage-contract.test.js` |
| D15 | done | Hardened release engineering with tarball-contract verification, checksum generation, hosted release workflow scaffolding, stronger vendored-binary smoke proof, and a dynamic-version Ubuntu full-work proof script | `scripts/verify-npm-pack.mjs`, `scripts/generate-checksums.mjs`, `.github/workflows/release.yml`, `scripts/verify-vendored-binary.mjs`, `scripts/verify-ubuntu-docker-full.mjs`, `tests/node/verify-npm-pack.test.js`, `tests/node/generate-checksums.test.js`, `tests/node/release-workflow-contract.test.js` |
| D16 | done | Added a canonical source release-bundle builder so local/CI packaging emits one cleaned artifact directory with archive, verification report, checksums, and manifest instead of relying on stale `dist/` state | `scripts/build-release-artifacts.mjs`, `.github/workflows/release.yml`, `tests/node/build-release-artifacts.test.js`, `docs/release-engineering.md`, `README.md` |
| D17 | done | Added dynamic client catalog loading, `client install all`, and scheduler-visible project/state-profile/desktop/credential routing policy fields | `src/client_catalog.rs`, `src/client/actions.rs`, `src/client/plan.rs`, `src/server/model.rs`, `src/server/loader.rs`, `mcpace.config.json`, `docs/universal-runtime-policy.md` |
| D18 | done | Added file-backed runtime lease enforcement with acquire/renew/release/list, stale lock recovery, MCP management tools, and conflict tests for host-lock, project-local, and bounded-parallel lanes | `src/hub/leases.rs`, `src/hub/args.rs`, `src/hub.rs`, `src/mcp_server.rs`, `tests/hub_leases.rs`, `tests/mcp_server.rs`, `docs/universal-runtime-policy.md` |
| D19 | done | Added automatic rollback backups for config-writing `client install` plus `client restore <client\|all> --backup <id\|latest>` so local config onboarding is previewable and undoable | `src/client/actions.rs`, `src/client/args.rs`, `tests/client_surface.rs`, `README.md` |
| D20 | done | Attached scheduler leases to explicit live upstream forwarding so HTTP `/mcp` and stdio fallback `upstream_call` / `upstream_batch` acquire, heartbeat-renew, use, and release leases around real stdio upstream MCP calls while ignoring stale JSON-RPC ids, conservatively leasing settings-only servers, and cancelling on lost heartbeat | `src/upstream.rs`, `src/hub/leases.rs`, `src/dashboard.rs`, `src/mcp_server.rs`, `tests/mcp_server.rs` |
| D21 | done | Added grouped top-level `release build` as a native Rust wrapper over the existing local artifact/proof bundle, keeping output honest that it does not publish to npm/GitHub | `src/release.rs`, `src/app.rs`, `src/catalog.rs`, `tests/help_and_root.rs`, `reports/rust-command-coverage.*` |
| D22 | done | Hardened JSON facade tests around non-ASCII content, malformed numbers/escapes, lone surrogate rejection, and number text normalization | `src/json.rs` |
| D23 | done | Expanded readiness prerequisite reporting beyond Docker by deriving runtime prerequisites from enabled server kinds, catalog `requiredCommands`, and enabled stdio source commands | `src/doctor.rs`, `src/verify/model.rs`, `src/verify/render.rs`, `tests/config_and_server.rs` |
| D24 | done | Added scoped lease takeover semantics so a restarted same-session owner can replace its own conflicting lease while cross-session takeover remains blocked | `src/hub/args.rs`, `src/hub/leases.rs`, `tests/hub_leases.rs` |
| D25 | done | Broadened real-looking client config merge safety coverage for JSON and YAML installs, including user-owned Claude permissions/hooks, existing MCP servers, Hermes comments, later YAML sections, and restore round-trip | `tests/client_surface.rs` |
| D26 | done | Removed stale README underclaims about `release` remaining planned now that `release build` exists, and added a docs contract so future docs keep the no-publish boundary explicit | `README.md`, `tests/node/docs-contract.test.js` |
| D27 | done | Synced the capability inventory with completed config-merge safety work so `adapter-config-merge-safety` is implemented instead of stale planned, and locked that with a fixture contract | `eval/runtime-capabilities.json`, `tests/node/fixtures-contract.test.js`, `reports/verification-latest.json` |
| D28 | done | Added non-destructive Codex TOML diagnostics for the original MCP startup-failure class: preserved non-MCPace stdio entries now warn when their `command` is missing on PATH instead of letting users assume MCPace caused the startup failure | `src/client/actions.rs`, `tests/client_surface.rs`, `README.md`, `eval/runtime-capabilities.json` |
| D29 | done | Surfaced the same broken-client-config class through `verify readiness` / `verify doctor` as `clientConfigWarnings`, keeping runtime readiness separate from client-startup warnings | `src/doctor.rs`, `src/verify/model.rs`, `src/verify/render.rs`, `tests/config_and_server.rs` |
| D30 | done | Consolidated Codex TOML inspection behind one small parser so install diagnostics and readiness warnings agree on table names, comments, quotes, and escaped command strings | `src/codex_config.rs`, `src/client/actions.rs`, `src/doctor.rs`, `tests/client_surface.rs`, `tests/config_and_server.rs` |
| D31 | done | Hardened local runtime tests that share host ports or forced lease-loss timing by serializing live-server unit tests and giving the lease-loss integration enough TTL headroom | `src/lib.rs`, `src/serve.rs`, `src/dashboard.rs`, `tests/mcp_server.rs` |
| D32 | done | Added a derived active session registry to the file-backed lease store so `hub lease list --json` reports `activeSessionCount` and per-session active lease ids after acquire/renew/release/list and expiry pruning | `src/hub/leases.rs`, `tests/hub_leases.rs` |
| D33 | done | Hardened `serve start/status` health detection so an arbitrary TCP listener is not mistaken for MCPace, and serialized setup integration tests that allocate local ports | `src/serve.rs`, `tests/setup.rs` |
| D34 | done | Surfaced active lease/session counts in `hub status` so operators can see scheduler occupancy without opening the raw lease list | `src/hub/model.rs`, `src/hub/status.rs`, `tests/hub_runtime.rs`, `tests/hub_leases.rs` |
| D35 | done | Added bounded in-process upstream session pooling behind `upstream_call` / `upstream_batch` so same-context stdio calls can reuse an initialized upstream process without expanding the top-level MCP tool surface | `src/upstream.rs`, `src/mcp_server.rs`, `src/dashboard.rs`, `tests/mcp_server.rs`, `docs/codex-mcpace-guide.md` |
| D36 | done | Derived HTTP `/mcp` upstream-wrapper affinity from explicit args, metadata hints, and MCP/bridge headers so native client session ids can partition pooled state without requiring a larger tool surface | `src/dashboard.rs`, `docs/codex-mcpace-guide.md`, `docs/client-metadata-routing.md` |
| D37 | done | Corrected the Windows desktop MCP lane to use an explicit stdio template (`uvx windows-mcp`) with no autostart and activation through a dedicated `desktop` profile, instead of a guessed HTTP endpoint that MCPace cannot fan out to yet | `mcp_settings.json`, `mcpace.config.json`, `manager.settings.json`, `docs/universal-runtime-policy.md`, `tests/node/fixtures-contract.test.js` |
| D38 | done | Added config-driven MCPace bridge guardrails so any upstream server can declare sensitive `toolPolicies` in `mcpace.config.json`; `windows-mcp` now uses that policy to require per-request opt-in before screenshot/snapshot/scrape, keyboard/window control, and PowerShell/filesystem/clipboard/process/registry tools are launched | `src/upstream.rs`, `src/mcp_server.rs`, `src/dashboard.rs`, `mcpace.config.json`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md` |
| D39 | done | Reclassified popular stateful upstreams from optimistic multi-reader defaults to conservative config-driven policies after live `tools/list` showed missing trusted annotations: `sequential-thinking` is session-affine, `memory` and `filesystem` are single-writer stores, and memory/filesystem writes require explicit mutation risk authorization | `mcpace.config.json`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md`, `tests/node/fixtures-contract.test.js` |
| D40 | done | Added and live-probed additional MCP canaries: `time` is a safe default, while `git`, `everything`, `sqlite`, and `playwright` are source-enabled behind `labs`; git/sqlite/Playwright mutation and interaction-control tools are guarded by declarative risk policies | `mcp_settings.json`, `mcpace.config.json`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md`, `tests/node/fixtures-contract.test.js` |
| D41 | done | Added `upstream_policy_audit` so any configured MCP server can be reviewed from live `tools/list` output for ToolAnnotations, generic advisory risks, matching declarative `toolPolicies`, unprotected guard-recommended tools, and unknown semantics without hardcoding popular servers into Rust enforcement; used it to guard active external MCP server controls, Lean edit/shell, and Serena source/memory mutations declaratively | `src/upstream.rs`, `src/mcp_server.rs`, `src/dashboard.rs`, `mcpace.config.json`, `tests/mcp_server.rs`, `tests/node/fixtures-contract.test.js`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md` |
| D42 | done | Added `upstream_policy_suggest` so future MCPs can automatically produce dry-run `toolPolicies` candidates from audit signals with generated risk-class/allow-argument names instead of requiring manual pattern discovery or Rust hardcoding | `src/upstream.rs`, `src/mcp_server.rs`, `src/dashboard.rs`, `tests/mcp_server.rs`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md` |
| D43 | done | Added `surface_manifest` so MCPace honestly reports the exact native top-level tool surface versus proxied upstream MCP tools, including optional live upstream catalog counts, instead of relying on a small `tools/list` without explaining the wrapper/proxy contract | `src/upstream.rs`, `src/mcp_server.rs`, `src/dashboard.rs`, `tests/mcp_server.rs`, `docs/codex-mcpace-guide.md`, `docs/universal-runtime-policy.md`, `docs/adr/0003-upstream-tool-surface-and-session-pooling.md` |
| D44 | done | Added the runtime performance/resource pass: bounded parallel HTTP request handling, local API/diagnostic fan-out, cgroup-aware defaults, request size/time guards, bounded upstream discovery/probe workers, dynamic upstream session-pool sizing, release-profile optimizations, and a dedicated performance doc | `src/resources.rs`, `src/dashboard.rs`, `src/serve.rs`, `src/setup.rs`, `src/service.rs`, `src/upstream.rs`, `Cargo.toml`, `docs/runtime-performance.md`, `tests/node/docs-contract.test.js` |
| D45 | done | Added the second performance pass: dashboard overview cache/coalescing with refresh bypass, HTTP header-count guard, queue-based bounded upstream workers to remove batch head-of-line blocking, per-key `tools/list` singleflight, resource-flag propagation through autostart/status state, and a performance decision log | `src/resources.rs`, `src/dashboard.rs`, `src/serve.rs`, `src/setup.rs`, `src/service.rs`, `src/upstream.rs`, `src/app.rs`, `docs/runtime-performance.md`, `docs/performance-decision-log-20260430.md`, `README.md`, `tests/node/docs-contract.test.js` |
| D46 | done | Added the third performance pass: replaced per-request local HTTP thread spawning with a fixed rendezvous-backed worker pool, added health/readiness caching plus runtime HTTP/session-pool counters, capped the short upstream `tools/list` cache to bound memory, made manual dashboard refreshes bypass the cache intentionally, surfaced overview-cache status in the UI, and updated the performance contracts | `src/dashboard.rs`, `src/upstream.rs`, `docs/runtime-performance.md`, `docs/performance-decision-log-20260430.md`, `README.md`, `tests/node/docs-contract.test.js` |
| D47 | done | Added the fourth performance pass: cached `/healthz` readiness responses with explicit refresh bypass, stale overview/health fallback metadata, live runtime telemetry for HTTP worker counters/resource limits/cache TTLs/upstream session-pool occupancy, and docs/tests for the observability contract | `src/resources.rs`, `src/dashboard.rs`, `src/upstream.rs`, `src/app.rs`, `src/serve.rs`, `docs/runtime-performance.md`, `docs/performance-decision-log-20260430.md`, `README.md`, `tests/node/docs-contract.test.js` |
| D48 | done | Added the fourth-plus performance pass: sharded the HTTP upstream session pool by context to reduce one-mutex contention, exposed `/api/resources` for live resource telemetry, added `scripts/benchmark-runtime.mjs` / `npm run benchmark:runtime` for repeatable local smoke measurements, and locked the new runtime contract with Node tests | `src/dashboard.rs`, `src/upstream.rs`, `src/resources.rs`, `scripts/benchmark-runtime.mjs`, `tests/node/runtime-performance-contract.test.js`, `docs/runtime-performance.md`, `README.md`, `package.json` |
| D49 | done | Hardened the proof-report runner against verbose child command output by adding a configurable `MCPACE_PROOF_COMMAND_MAX_BUFFER_BYTES` spawn buffer, reporting the buffer in command metadata, and locking the contract with a Node test so source/release proof does not fail just because `npm test` is chatty | `scripts/proof-report.mjs`, `tests/node/proof-report.test.js`, `docs/verification-matrix.md`, `docs/test-strategy.md` |
| D50 | done | Added the source/architecture quality pass: deterministic `audit:source` CI lane, explicit protocol/resource boundary checks, production-hazard scanning, durable JSON audit artifacts, structured dashboard/API JSON 500 error boundary, and extra Node/Rust tests around the new contracts | `scripts/audit-source.mjs`, `scripts/run-rust-tests.mjs`, `tests/node/source-quality-contract.test.js`, `src/dashboard.rs`, `docs/source-quality.md`, `docs/architecture-boundaries.md`, `docs/test-strategy.md`, `README.md`, `package.json` |
| D51 | done | Centralized Unix detach unsafe/FFI into a reviewed boundary module, made source audit fail any new production unsafe outside approved process modules, added an ordered `verify:rust-quality` host gate with fmt/Clippy/tests/release-build lanes, and exposed optional built-in Node coverage | `src/process_detach.rs`, `src/hub/launcher.rs`, `src/serve.rs`, `scripts/audit-source.mjs`, `scripts/verify-rust-quality.mjs`, `tests/node/source-quality-contract.test.js`, `tests/node/rust-quality-contract.test.js`, `tests/node/coverage-contract.test.js`, `.github/workflows/ci.yml`, `docs/source-quality.md` |


## Prioritized open backlog

| Pri | ID | Status | Work | Points | Depends on | Definition of done | Risks | ETA range |
|---|---|---|---|---:|---|---|---|---|
| P0 | R1 | in progress | Promote bootstrap-only `mcpace stdio-shim` into a live stdio ingress that normalizes initialize metadata and forwards into the persistent hub | 13–21 | planner context resolution, hub lifecycle shell | `stdio` launcher forwards live MCP traffic; `_meta`/cwd/roots normalization matches planner; cancellation path tested; no overclaim in docs | host-specific stdio quirks; session stickiness bugs | 1–3 weeks after Rust host is available |
| P0 | R2 | partial | Ship local Streamable HTTP ingress with localhost-only defaults and MCP session handling | 13–21 | R1 optional, hub lifecycle shell | local HTTP endpoint exists with bounded parallel request workers, request body/timeout guards, origin/binding rules, and explicit/metadata/header session hints; remaining: protocol-level session create/reuse/close covered and readiness docs updated | transport drift from spec baseline; auth/session edge cases | 2–4 weeks |
| P0 | R3 | partial | Enforce scheduler leases for `single-session` / `shared-exclusive` / state-profile / project-local servers | 8–13 | current planner warnings | file-backed acquire/renew/release/list exists with TTL, conflict checks, request-time upstream forwarding gates, conservative settings-only leases, heartbeat renewal, lost-heartbeat cancellation, scoped same-session takeover, a derived active-session registry, bounded in-process upstream session pooling, resource-derived pool/task bounds, and tests; remaining: durable cross-process ownership and richer per-upstream policy tuning | stale ownership in future pooled sessions, deadlocks, restart races | next runtime pass |
| P0 | R4 | partial | Add cancel/restart guards so dead leases cannot accept stale results | 8–13 | R3, ingress work | request-time wrapper calls now ignore stale JSON-RPC ids, cancel before accepting a result after lease heartbeat loss, reuse pooled sessions only for matching context/config keys, and expose active lease sessions for restart/cancel bookkeeping; remaining: transport-level cancel propagation, adversarial lab fixtures for pooled sessions, and pool invalidation on explicit runtime cancellation | subtle race conditions and transport mismatch | 1–2 weeks |
| P0 | T1 | in progress | Freeze product truth for the next cycle: first ICP, one-sentence promise, support tiers, capability `claimStatus`, and beta gate | 2–3 | current docs, capability inventory | README, `STATE.md`, roadmap docs, and `eval/runtime-capabilities.json` all use the same truth taxonomy; tier-1 surfaces and beta gate are named explicitly; docs stop collapsing north-star language into current support claims | docs drift faster than code; marketing breadth outruns proof | 1–3 days |
| P1 | A1 | in progress | Finish `client export` as a manifest-driven config patcher after the now dry-run/diff/restore-capable install path | 5–8 | current client catalog, owned-block patch rules | at least one blocked/public surface can export through a config-writing path; install dry-run/diff/restore remains covered; docs stay honest about which clients still remain preview-only | config shape drift across clients | 1–3 weeks |
| P1 | A2 | done | Prove config-merge safety and owned-block patching against real-looking client configs | 5–8 | A1 | fixtures include mixed user-managed JSON/YAML config; patcher preserves non-MCPace blocks, comments, later YAML sections, and rollback path restores the original config | destructive config writes; incomplete client samples | completed in current Ralph pass |
| P1 | M1 | done | Finish grouped top-level `release` command | 3–5 | archive builder | `release build` exists, is documented honestly, and only claims local source/artifact proof without npm/GitHub publication | pressure to overclaim publish readiness | completed in current Ralph pass |
| P1 | Q1 | done | Harden custom JSON parser with focused correctness tests | 3–5 | none | tests cover surrogate pairs, leading-zero rejection, malformed numbers/escapes, non-ASCII strings, and number text normalization | parser bugs remain hidden until runtime inputs get wider | completed in current Ralph pass |
| P1 | Q2 | done | Expand `doctor`/`verify readiness` prerequisite mapping beyond Docker as more runtime kinds appear | 3–5 | R1/R2 design stability | readiness now reports missing runtime prerequisites from container kinds, catalog commands, and enabled stdio source commands; tests cover non-container missing source command explanation | too-early generalization before ingress contracts settle | completed in current Ralph pass |
| P1 | B1 | partial | Re-run build proof across supported OS lanes | 8–13 | Rust toolchain, CI hosts | `npm run verify:rust-quality` is the canonical host gate for `cargo fmt --all -- --check`, `cargo clippy --all-targets --locked -- -D warnings`, suite-isolated Rust tests, and `cargo build --release --locked`; remaining: full run on Linux/macOS/Windows hosts | host drift; CI lane availability | 1–2 weeks once remaining hosts exist |
| P1 | B2 | blocked | Re-run runtime proof on Ubuntu/macOS/Windows with real prerequisites | 8–13 | R1–R4 at least partially done, supported hosts | `verify doctor`, `verify readiness`, and live ingress smoke pass on real hosts; results written to verification report | Docker/client availability, platform-specific behavior | 2–4 weeks after runtime core lands |
| P1 | E1 | in progress | Keep prompt/agent evals production-like, split by typical/edge/adversarial/held-out, and tied to real regressions | 5–8 | current eval docs and fixtures | every fixture has grounding, binary checks, rubric dimensions, and held-out handling; regression loop is written down and machine-checked | easy to slip into showroom evals without real traffic/traces | 3–6 days for baseline; ongoing refresh afterwards |
| P1 | E2 | blocked | Add sanitized historical traces and real-host cases to eval datasets | 5–8 | B2, safe trace capture process | held-out set includes sanitized real failures; no secrets/PII; source of each case documented | access to logs, privacy review, inconsistent trace quality | 1–3 weeks after traces exist |
| P2 | C1 | blocked | Build a real-client compatibility matrix for Codex, Claude, Cursor, Kiro, Windsurf, Copilot, Gemini, Hermes | 8–13 | B2, real client access | initialize/session/roots/auth traces captured; compatibility notes updated; at least one held-out case per major surface | closed/cloud surfaces may be inaccessible in CI | 2–5 weeks |
| P2 | P1 | not started | Add `profile` mutations and `projects` scan/write flows only after runtime core is stable | 5–8 | runtime core maturity | write paths exist with tests and docs; no silent mutation of user state | low immediate product value vs runtime core | 1–2 weeks |

## Ordering notes

1. The next best runtime step is now **hardening pooled ownership beyond the in-process slice**: keep the new resource-derived HTTP/upstream bounds, then add durable cross-process pool ownership, transport-level cancel propagation, richer per-upstream backpressure policy, and adversarial pooled-session lab fixtures around the already lease-gated/lost-heartbeat-guarded wrapper calls, active-session registry, and `upstream_policy_audit` / `upstream_policy_suggest` review loop.
2. **T1** should run in parallel so README, `STATE.md`, roadmap docs, and the capability inventory stop drifting while runtime work continues.
3. **A1/A2** should wait until the ingress/lease contract is stable enough that client onboarding is not built on a moving target.
4. **B1/B2/C1/E2** are real blockers, not paper tasks. They depend on environments and traces that are not present in this container.
5. **Q2** matters because the latest readiness fix intentionally handles only the container-backed runtime slice that is already visible in repo fixtures/configs.
