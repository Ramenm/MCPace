# Legacy cleanup pass

This bundle keeps the public source tree focused on the current MCPace runtime surface.

Retired bridge/config surfaces remain excluded from the release manifest, including `manager.settings.json`, old manager bridge flags, stale generated-code references, heavyweight build outputs, caches, and runtime logs. The remaining docs describe the compact user-facing flow: `mcpace up`, explicit server import/install, policy review, dashboard visibility, and validation.
