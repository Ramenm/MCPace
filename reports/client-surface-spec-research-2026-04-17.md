# Client surface research evidence

This evidence note records the maintained client-surface catalog coverage used by the runtime capability inventory. MCPace treats configured upstreams generically through MCP stdio or Streamable HTTP transports and does not enable a branded or vendor-specific bridge by default.

Reviewed surfaces: local JSON/TOML/YAML MCP client config targets, local hub-owned stdio entrypoints, dynamic upstream discovery, and generic projected upstream tools/resources/prompts.
