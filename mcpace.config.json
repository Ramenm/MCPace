{
  "name": "mcpace",
  "version": "0.6.9",
  "ports": {
    "hub": 12223,
    "serve": 39022
  },
  "hub": {
    "containerName": "mcpace",
    "image": "samanhappy/mcphub:0.12.12",
    "env": {
      "REQUEST_TIMEOUT": "180000"
    }
  },
  "packages": {
    "mcpRemote": "mcp-remote@0.1.18"
  },
  "profiles": {
    "runtime": {
      "default": "manual",
      "profiles": {
        "manual": {
          "description": "No upstream MCP servers are enabled by default. Add any MCP server to mcp_settings.json; add mcpace.config.json policy only when needed.",
          "serverOverrides": {}
        },
        "labs": {
          "description": "Empty sandbox profile; use it for local experiments by adding explicit serverOverrides.",
          "serverOverrides": {}
        }
      }
    }
  },
  "projects": {
    "discovery": {
      "roots": []
    }
  },
  "workspaces": {
    "primary": {
      "name": "primary",
      "hostPath": ".",
      "access": "rw"
    },
    "extras": []
  },
  "servers": {},
  "client": {
    "keyName": "MCPace"
  },
  "health": {
    "probeTimeoutSec": 2,
    "startupTimeoutSec": 60,
    "offlineThreshold": 2
  },
  "platform": {
    "dockerHostAlias": "host.docker.internal",
    "windowsOnlyServers": []
  },
  "compatibility": {
    "sessionGate": {
      "enabled": true,
      "streamProbeMs": 0,
      "idleDelayMs": 1500,
      "logTailLines": 400
    }
  },
  "clientCatalog": {
    "paths": [],
    "targets": [],
    "description": "Optional external client target catalog. Built-ins remain a fallback; entries here or MCPACE_CLIENT_CATALOG can add/override clients without recompiling."
  },
  "serve": {
    "host": "127.0.0.1",
    "port": 39022,
    "mcpPath": "/mcp",
    "publicUrl": "",
    "description": "Advertised Streamable HTTP endpoint. publicUrl overrides local host/port/path for client install/export when a relay or public ingress is used."
  },
  "mcpSettings": {
    "includePaths": [],
    "description": "Optional additional MCP settings JSON files/directories. Sources are merged in order: mcp_settings.json, default mcp_settings.d/*.json, includePaths/includeDirs, MCPACE_MCP_SETTINGS, then MCPACE_MCP_SETTINGS_DIRS; later duplicate server names override earlier ones.",
    "includeDirs": [
      "mcp_settings.d"
    ]
  },
  "autoProfile": {
    "description": "Evidence-first server profiling. MCPace derives conservative scheduling from source config, transport, launcher, live initialize/tools-list probes, and optional operator policy overrides; no packaged upstream-server catalog is required.",
    "defaultUnknownPolicy": "review-required + disabled-until-user-confirms + single-writer",
    "probeCalls": [
      "initialize",
      "notifications/initialized",
      "tools/list"
    ],
    "stateRules": "Infer runtimeType/stateClass/effectClass automatically: stateless read-only tools may share; session-stateful memory/context tools bind to chat/session; project-stateful file/git/db tools bind to project/worktree; credential/remote servers bind to credential or transport session; browser/shell/process servers stay exclusive; unknown stdio stays conservative until live probe evidence improves it."
  },
  "executionDefaults": {
    "protocol": "mcpace.execution.v1",
    "mode": "serialized",
    "affinity": [
      "client"
    ],
    "queueTimeoutMs": 10000,
    "reusePolicy": "sticky",
    "maxWorkers": 1,
    "maxInFlightPerWorker": 1,
    "description": "Safe default for unknown local stdio servers: one in-flight call per server until an explicit policy proves stronger concurrency is safe."
  },
  "uiSurface": {
    "enabled": true,
    "surface": "dashboard-http",
    "localOnly": true,
    "refreshIntervalMs": 15000,
    "showConcurrencyMap": true,
    "showAuditTrail": true,
    "auditTail": 60,
    "description": "Local-first operational surface for watching live leases, planned instances, concurrency lanes, and tool-call audit events. Desktop tray can remain a thin launcher later."
  },
  "approvedCatalog": {
    "paths": [
      "./catalog/approved-servers.json"
    ],
    "requireApprovalForUnknown": false,
    "trustLevelDefault": "review",
    "description": "Optional local allow/review catalog. Keep it advisory by default so MCPace remains lightweight; turn requireApprovalForUnknown on for stricter teams."
  },
  "dynamicDiscovery": {
    "enabled": true,
    "mode": "auto",
    "registryEndpoints": [
      "https://registry.modelcontextprotocol.io"
    ],
    "catalogPaths": [
      "./catalog/approved-servers.json"
    ],
    "registryCachePath": "./catalog/registry-cache.json",
    "refreshRegistryOnDiscover": false,
    "autoInstall": "trusted-only",
    "installUnknown": "plan-only",
    "probeAfterInstall": true,
    "description": "One-command dynamic mode. `mcpace auto` refreshes the registry cache when it is missing or stale, selects approved/trusted candidates, writes server config, launches the package manager only during probe, reads live initialize/tools/list evidence, and lets MCPace infer safe runtime policy without asking the user for a server type.",
    "maxAutoInstallsPerRun": 4,
    "autoRefreshRegistry": true,
    "registryCacheTtlHours": 24,
    "defaultCommand": "auto"
  }
}
